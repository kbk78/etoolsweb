<!DOCTYPE html>
<html lang="en">

<head>
  <title>etoolsweb</title>
<script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>
</head>

<body>

<div>
<label for="weatherFile">Select weather file</label>
<input name='weatherFile' type='file' id='f1' accept='.epw' onchange='openFile(event)'><br>
</div>


<script>
var openFile = function(event){
  var input = event.target;
  var reader = new FileReader();
  reader.readAsText(input.files[0]);
  reader.onload = function(){
   var text1 = reader.result.split('\n').slice(-8761).join('\n');
   values = d3.csvParseRows(text1,function(d,i){return +d[6]*1.8+32;});
	  return values;}

console.log(values)
</script>


<div id = "app-6">
<input type="range" min=1 max=100 value=20 class='slider' id="myrange" v-model="bins">  
<br>
<input v-model="bins" placeholder="edit me">
<p>Message is: {{ bins }}</p>
</div>

<script>
var app6 = new Vue({
  el: '#app-6',
  data: {
    bins: 20
  }
})
</script>

</body>
</html>
